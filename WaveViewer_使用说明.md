# WaveViewer 使用说明

WaveViewer 用于打开和查看 `.wvz4` 波形文件。本文只说明用户操作，不涉及内部实现。

## 省流版：打开文件并加入波形

### 1. Viewer 在哪

可执行文件位于：

```text
WaveTracer/QtViewer/build/x64/Release/WaveViewer.exe
```

直接双击 `WaveViewer.exe` 启动。

### 2. 打开波形

任选一种方式：

1. 点击顶部工具栏最左侧的“打开波形文件”按钮，选择 `.wvz4` 文件。
2. 从资源管理器把 `.wvz4` 文件直接拖进 Viewer 窗口。
3. 在命令行中传入文件路径：

```bat
WaveViewer.exe D:\wave\example.wvz4
```

打开成功后，窗口标题和顶部时间范围会显示当前文件信息。

### 3. 加入要看的信号

左侧是“可选信号”，中间是“选中信号”，右侧是波形区。

- 展开左侧层级树，双击叶子信号，即可加入中间列表。
- 也可在左侧选中一个或多个信号，直接拖到中间列表。
- 双击左侧的层级节点，可展开或折叠。
- 新打开文件时，Viewer 会先放入前几个信号，可按需删除或替换。

### 4. 立即开始看

- 鼠标滚轮：以鼠标所指时间为中心缩放。
- 左键点击：放置光标，中间列表显示该时刻的信号值。
- 左键拖出时间范围：放大到该范围。
- 中键拖动：左右平移当前视图。
- 点击工具栏“重置视图”：回到全局时间范围。

## 进阶版：按钮、鼠标和快捷键

### 1. 工具栏按钮

按工具栏从左到右的顺序：

| 按钮 | 用途 |
| --- | --- |
| 打开波形文件 | 打开一个 `.wvz4` 文件。 |
| 比较波形文件 | 第一步选择 1～2 个基线文件，第二步选择目标文件；可先过滤随机不稳定信号。 |
| Create temporary signal | 用已有信号和表达式创建临时信号。 |
| 放大 | 缩小当前显示的时间范围。 |
| 缩小 | 扩大当前显示的时间范围。 |
| 左移 | 将当前视图向左平移。 |
| 右移 | 将当前视图向右平移。 |
| 上一次变化 | 跳到中间列表当前选中信号的上一个变化点。 |
| 下一次变化 | 跳到中间列表当前选中信号的下一个变化点。 |
| 重置视图 | 清除光标并恢复全局时间范围。 |

把时间输入顶部“跳转时间”框后按 `Enter`，可直接跳到该时刻。可输入范围以顶部显示的整体时间范围为准。

### 2. 可选信号：层级树与搜索

- 在搜索框输入层级名或信号名，按 `Enter` 执行搜索。
- `Aa` 按钮：区分大小写。
- `.*` 按钮：把搜索内容按正则表达式解释。
- 搜索结果会展开匹配信号的上层路径，并定位到第一个结果。
- 按住 `Ctrl` 或 `Shift` 可在树中选择多个信号，然后一次拖入中间列表。

### 3. 选中信号：排序、删除和显示格式

- 直接上下拖动中间列表的信号，可调整波形行顺序。
- 按住 `Ctrl` 或 `Shift` 可多选。
- 按 `Delete` 删除选中信号；也可右键选择“删除选中信号”。
- 将中间列表的信号拖回左侧树，也可从当前显示中移除。
- 右键信号可选择 `bin`、`hex` 或 `dec`。
- 32 位信号还可选 `int`、`uint`、`float`；64 位信号还可选 `int64`、`uint64`、`double`。
- 在“搜索选中信号”中输入文字，可定位并高亮匹配行。
- `Ctrl+C` 会复制选中行的“信号名 + 当前值”，多行时按当前显示顺序复制。

### 4. 波形区鼠标操作

| 操作 | 结果 |
| --- | --- |
| 滚轮向上 / 向下 | 以鼠标所指时间为中心放大 / 缩小。 |
| 左键点击 | 放置时间光标；点在信号行上时同时选中该行。 |
| `Ctrl` + 左键点击信号行 | 在已有选择中加入或取消该行。 |
| 左键拖出范围 | 放大到选中时间范围。 |
| 中键拖动 | 左右平移当前视图。 |
| 底部全局条点击 | 把当前视图快速移到点击位置。 |
| 底部全局条拖动蓝色视窗框 | 在整个波形范围内快速平移。 |

### 5. 查找指定数值

1. 在中间列表高亮一个或多个信号。
2. 按 `Ctrl+F`，输入目标值。
3. 可输入十进制、十六进制、二进制或负数，例如 `10`、`0xA`、`0b1010`、`-1`。
4. 结果会显示每个信号的命中次数、首次命中时间和持续时间比例。
5. 点击 `Previous` / `Next` 在命中之间跳转；双击结果行跳到该信号的首次命中。

### 6. 创建临时信号

点击 `Create temporary signal` 按钮，或按 `Ctrl+E`：

1. 输入临时信号名。
2. 输入表达式；可先在主窗口选信号，再点 `Insert selected` 插入完整信号名。
3. `Width` 为 `auto` 时自动确定宽度，也可手动设置 1–64 位。
4. 可用运算符：`+ - * / % & | ^ ~ << >> && || ! == != < <= > >=`。
5. 点击 `Create`，新信号会加入当前波形。

临时信号只存在于当前 Viewer 会话中。

### 7. 快捷键汇总

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl+F` | 在中间列表当前高亮信号中查找指定数值。 |
| `Ctrl+E` | 打开“Create temporary signal”窗口。 |
| `Ctrl+[` | 跳到左侧树当前选中信号在当前视图内的第一个变化点。 |
| `Ctrl+]` | 跳到左侧树当前选中信号在当前视图内的最后一个变化点。 |
| `Ctrl+A` | 中间列表获得焦点时，全选已加入信号。 |
| `Ctrl+C` | 中间列表获得焦点时，复制选中信号名和当前值。 |
| `Delete` | 中间列表获得焦点时，删除选中信号。 |
| `Enter` | 在左侧搜索框中执行搜索；在跳转时间框中执行跳转；在数值查找框中执行查找。 |

补充：在波形比较结果中，按住 `Ctrl` 同时点击“上一次变化”或“下一次变化”，可只在差异点之间跳转。

### 5. 随机性过滤与事件时刻比较

点击比较按钮后：

1. 第一步选择一个或两个基线文件。选择一个时，直接以该文件为 A 与目标文件 B 比较。
2. 如果选择两个期望相同的基线文件，Viewer 会先严格比较它们；数值或事件时刻不同、以及只存在于第一个基线中的信号都会被视为不稳定信号。
3. 第二步选择目标文件。正式比较仍使用第一个基线作为 A，并跳过上一步发现的不稳定信号。
4. 勾选“正式比较只比较事件时刻，不比较数值”后，正式比较只检查同路径信号的事件时间序列；基线过滤仍严格比较事件时刻和数值。

比较只处理文件间都有的同路径信号。事件时刻模式不会因为同一时刻的值不同而报告差异，但事件提前、延后、增加或缺失仍会报告差异。

比较结果加入“选中信号”区时，会自动按首次差异时间升序排列；同一信号的 A/B 两侧保持原有稳定顺序，无需手动点击排序按钮。

## 使用建议

- 信号较多时，先用左侧搜索定位，再批量拖入。
- 查看局部细节时，优先用左键框选时间范围；返回全局时使用“重置视图”。
- 跳变定位前，先在中间列表选中目标信号；需要联合观察时可多选。
- 需要对照两次运行结果时，直接使用“比较两个波形文件”，无需分别打开。
